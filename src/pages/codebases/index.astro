---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Breadcrumbs from '../../components/Breadcrumbs.astro';
import { getCollection } from 'astro:content';
import { breadcrumbSchema, itemListSchema } from '../../utils/jsonld';

const codebases = await getCollection('codebases');
const sortedCodebases = codebases.sort((a, b) => a.data.order - b.data.order);

const jsonLd = [
  breadcrumbSchema([
    { name: 'Home', url: '/' },
    { name: 'Codebases', url: '/codebases' }
  ]),
  itemListSchema(
    sortedCodebases.map(c => ({
      name: c.data.title,
      url: `/codebases/${c.slug}`,
      description: c.data.description
    })),
    'Available Codebases'
  )
];

const statusLabels: Record<string, { text: string; class: string }> = {
  'available': { text: 'Available', class: 'bg-green-100 text-green-700' },
  'limited': { text: 'Limited', class: 'bg-yellow-100 text-yellow-700' },
  'in-development': { text: 'In Development', class: 'bg-blue-100 text-blue-700' },
};
---

<BaseLayout
  title="Codebases"
  description="Deploy-ready codebases for security, compliance, and infrastructure. Buy complete, documented repositories with support included."
  jsonLd={jsonLd}
  realmGlow="0.64 0.17 190"
  realmBg="0.12 0.01 220"
  realmText="0.98 0.01 220"
>
  <!-- Hero -->
  <section class="border-b border-white/10">
    <div class="container-wide section-padding-sm">
      <div class="max-w-3xl">
        <Breadcrumbs items={[
          { label: 'Home', href: '/' },
          { label: 'Codebases' },
        ]} />
        <h1 class="text-4xl sm:text-5xl font-bold mb-4">
          Codebases for Sale
        </h1>
        <p class="text-xl opacity-70 mb-6">
          Deploy-ready repositories with documentation, tests, and support. 
          Skip months of development and get something that works today.
        </p>
        <div class="flex flex-wrap gap-4">
          <a href="#bundle" class="btn-primary">
            View Bundle Deal
          </a>
          <a href="/contact?subject=Codebase%20Inquiry" class="btn-secondary">
            Request Custom Build
          </a>
          <a href="/blog/buying-a-codebase" class="btn-ghost">
            Read: Buying a Codebase →
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- Codebase Grid -->
  <section class="container-wide section-padding-sm">
    <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
      {sortedCodebases.map((codebase) => {
        const status = statusLabels[codebase.data.status] || statusLabels['available'];
        return (
          <a 
            href={`/codebases/${codebase.slug}`}
            class="card-interactive p-8 group flex flex-col"
          >
            <div class="flex items-start justify-between mb-4">
              <span class="text-xs font-medium opacity-50 uppercase tracking-widest">
                {codebase.data.category}
              </span>
              <span class={`text-xs font-medium px-3 py-1 rounded-full ${status.class}`}>
                {status.text}
              </span>
            </div>
            <h2 class="text-2xl font-bold group-hover:text-warm-500 transition-colors mb-3">
              {codebase.data.title}
            </h2>
            <p class="text-base opacity-70 mb-6 flex-grow">
              {codebase.data.tagline}
            </p>
            <div class="flex items-baseline gap-2 mb-4">
              <span class="text-2xl font-bold">
                ${codebase.data.price.toLocaleString()}
              </span>
              {codebase.data.originalPrice && (
                <span class="text-sm opacity-40 line-through">
                  ${codebase.data.originalPrice.toLocaleString()}
                </span>
              )}
            </div>
            {codebase.data.licensesAvailable && codebase.data.licensesAvailable <= 5 && (
              <div class="text-xs text-red-600 font-medium mb-2">
                ⚡ Only {codebase.data.licensesAvailable} left
              </div>
            )}
            <div class="flex flex-wrap gap-2">
              {codebase.data.tags.slice(0, 3).map((tag) => (
                <span class="text-xs px-2 py-1 rounded bg-white/10 opacity-70">
                  {tag}
                </span>
              ))}
            </div>
          </a>
        );
      })}
    </div>
  </section>

  <!-- What's Included -->
  <section class="bg-white/5 border-y border-white/10">
    <div class="container-wide section-padding">
      <div class="max-w-3xl mx-auto text-center">
        <h2 class="text-3xl font-bold mb-6">What You Get</h2>
        <p class="text-xl opacity-70 mb-10">
          Every codebase purchase includes everything you need to deploy and maintain.
        </p>
        <div class="grid sm:grid-cols-2 md:grid-cols-4 gap-6 text-left">
          <div class="bg-white/10 rounded-engine-inner p-6 border border-white/10 shadow-engine-soft">
            <h3 class="font-bold mb-2">Full Source</h3>
            <p class="text-sm opacity-70">Complete, unobfuscated source code</p>
          </div>
          <div class="bg-white/10 rounded-engine-inner p-6 border border-white/10 shadow-engine-soft">
            <h3 class="font-bold mb-2">Documentation</h3>
            <p class="text-sm opacity-70">Setup guides, API docs, architecture overview</p>
          </div>
          <div class="bg-white/10 rounded-engine-inner p-6 border border-white/10 shadow-engine-soft">
            <h3 class="font-bold mb-2">Test Coverage</h3>
            <p class="text-sm opacity-70">Automated tests you can run and extend</p>
          </div>
          <div class="bg-white/10 rounded-engine-inner p-6 border border-white/10 shadow-engine-soft">
            <h3 class="font-bold mb-2">Support Window</h3>
            <p class="text-sm opacity-70">30–90 days for setup/questions</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Bundle Deal -->
  <section id="bundle" class="bg-surface-900/50 backdrop-blur-md border-y border-white/10">
    <div class="container-wide section-padding">
      <div class="max-w-3xl mx-auto text-center">
        <h2 class="text-4xl font-bold mb-4">Buy the Bundle — Save $3,500</h2>
        <p class="text-xl opacity-70 mb-8">
          Get all three codebases for one price. Perfect if you're building a security and automation practice.
        </p>
        <div class="flex items-center justify-center gap-6 mb-10">
          <span class="text-5xl font-bold">$15,000</span>
          <span class="text-2xl opacity-40 line-through">$18,500</span>
          <span class="text-sm bg-green-600 text-white px-4 py-1 rounded-full font-bold">Save 19%</span>
        </div>
        <div class="grid sm:grid-cols-3 gap-6 text-left mb-10">
          <div class="bg-white/10 rounded-engine-inner p-6">
            <h3 class="font-bold mb-2">ComplyCrawl</h3>
            <p class="text-sm opacity-70">Compliance-first web automation</p>
          </div>
          <div class="bg-white/10 rounded-engine-inner p-6">
            <h3 class="font-bold mb-2">TallySec</h3>
            <p class="text-sm opacity-70">Security training platform</p>
          </div>
          <div class="bg-white/10 rounded-engine-inner p-6">
            <h3 class="font-bold mb-2">AegisTwin</h3>
            <p class="text-sm opacity-70">Agent runtime + governance</p>
          </div>
        </div>
        <a href="https://buy.stripe.com/28E6oHf2C6ha7Fp6IY87K09" class="btn-primary text-base px-10 py-4" target="_blank" rel="noopener">
          Buy Bundle — $15,000
        </a>
        <p class="opacity-50 text-sm mt-6">
          Includes all source code, documentation, and 90-day support for all three products.
        </p>
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="container-wide section-padding-sm">
    <div class="border-t border-white/10 pt-12 max-w-xl">
      <h2 class="text-3xl font-bold mb-4">Don't see what you need?</h2>
      <p class="text-lg opacity-70 mb-8">
        We can build it from scratch or adapt an existing codebase to your requirements. Custom builds start at $15,000.
      </p>
      <a href="/contact?subject=Custom%20Codebase" class="btn-primary">
        Request Custom Build
      </a>
    </div>
  </section>
</BaseLayout>
